import React from 'react';
import { Platform, StatusBar, StyleSheet, View } from 'react-native';
import TabNavigator from '../navigation/TabNavigator';


export default class TakeCarScreen extends React.Component {

  render() {
      return (
          <TabNavigator />
      );
  }
}